{
  "name": "eepub-calibre-helper",
  "version": "0.0.1",
  "private": true,
  "description": "Optional local helper for eEPUB that calls your locally installed Calibre (Mac/Windows) via localhost",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
